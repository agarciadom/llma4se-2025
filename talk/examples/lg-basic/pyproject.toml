[project]

name = "lg-basic"
version = "0.1.0"
description = "Basic examples for the LangGraph + smolagents talk"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langgraph>=0.6.6",
]
